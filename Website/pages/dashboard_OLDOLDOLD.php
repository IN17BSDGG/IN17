<?php
/*
$geodb = "SELECT * FROM BUNDESLAENDER";
$sth = $db->prepare($geodb);
$sth->execute();
$result = $sth->fetchAll();
foreach ($result as $item) {
    echo "Datensatz: ".$item['shape']."</br>"; // Make sure to unescape the hex blob
    $wkb = $item['shape'];
    //$geom = geoPHP::load($wkb, 'ewkb'); // We now a full geoPHP Geometry object
}
*/
?>

